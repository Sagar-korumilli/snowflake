CREATE OR REPLACE TABLE sales.customers (
    customer_id INT,
    name STRING,
    email STRING,
    created_at TIMESTAMP
);
CREATE OR REPLACE TABLE sales.customers2 (
    customer_id INT,
    name STRING,
    email STRING,
    created_at TIMESTAMP
);

