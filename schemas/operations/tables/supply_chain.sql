CREATE OR REPLACE TABLE operations.supply_chain (
    item_id INT,
    location STRING,
    quantity INT,
    last_updated DATE
);
CREATE OR REPLACE TABLE operations.supply_chain2 (
    item_id INT,
    location STRING,
    quantity INT,
    last_updated DATE
);
